{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run mypics main()",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/mypics.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "justMyCode": true
        }
    ]
}
